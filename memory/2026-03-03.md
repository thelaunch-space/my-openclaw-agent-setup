# Memory Log — 2026-03-03 (Tuesday)

## Critical Issue: Convex-First Failures

**Krishna reminded me 5 times in 6 days about Convex-first approach.** Pattern:
- I do partial fixes, not comprehensive audits
- I update docs but don't verify actual data flow
- I'm reactive (wait for Krishna to catch issues) instead of proactive

**Still reading from Sheets (as of today):**
| Script | Command | Reads From |
|--------|---------|------------|
| `vyasa-sheets-helper.js` | `list` | SHEETS ❌ |
| `vidura-sheets-helper.js` | `list-briefs` | SHEETS ❌ |
| `vidura-sheets-helper.js` | `list-questions` | SHEETS ❌ |
| Health check crons | call `list` | SHEETS ❌ |

**Fix needed:** Either remove these commands or add `/query/briefs?status=all` endpoint to Convex.

---

## Issue Reporting Protocol Added

Added to main AGENTS.md - mandatory for all issue reports:
1. What's broken
2. Root cause (investigate WHY)
3. Proposed fix

Updated health check crons (Morning 8 AM, Daily Due Diligence 7 PM) to require root cause analysis.

---

## Valmiki: Hook/CTA Selection

Krishna now selects specific hook and CTA on Kanban cards before approving.

**New fields returned by `/query/linkedin-posts?status=approved`:**
- `selectedHook` - if present, use EXACTLY (no picking different)
- `selectedCta` - if present, use EXACTLY (no hybrid)

Updated valmiki/AGENTS.md Phase 2 Step 2 and Step 5 report template.

---

## Shakti: New Endpoints

Added to shakti/TOOLS.md and AGENTS.md:
- `POST /update/task` - update title, description, paceNotes by _id
- `POST /delete/task` - permanently delete (Krishna explicit request only)

---

## Context Management Discussion

Krishna asked about context management. Key points explained:
- Agents run isolated cron sessions (fresh each run, no accumulation)
- Main session accumulates until `/new` or compaction
- Memory files (MEMORY.md, daily files) persist across sessions
- Recommended: `/new` at natural breakpoints, delete memory files >30 days old

---

## Vidura "Zero Publication" Investigation

Vidura reported "5 consecutive zero-publication days" but Vyasa WAS creating PRs (#66, #62, #58).

**Root cause:** Vidura tracks "Published" (merged + live), not "PR Created". The bottleneck was merge velocity (7 PRs waiting), not Vyasa's output.

Added GitHub PR queue check to Daily Due Diligence cron to catch this pattern.
