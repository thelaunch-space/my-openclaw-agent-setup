# Memory - 2026-04-07

## Session Events

### Cross-Agent Visibility Fix
- **Issue**: Agents were failing with Anthropic "out of usage" errors and OpenRouter auth failures
- **Root cause**: 
  1. Agents had `lastGood` pointing to Anthropic profiles (out of credits)
  2. OpenRouter fallback keys were stale/different from models.json
  3. GLM-5 wasn't in agent models.json files
- **Fix applied**:
  1. Updated `/home/node/.openclaw/openclaw.json`:
     - Added `tools.sessions.visibility: "all"` for cross-agent session history
     - Changed all agents to `model.primary: "openrouter/z-ai/glm-5"`
  2. Updated each agent's `auth-profiles.json` (vibhishana, vyasa, vidura, valmiki, shakti):
     - Set `lastGood.openrouter: "openrouter:default"`
     - Used working API key from models.json: `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`
  3. Added GLM-5 to each agent's `models.json`
- **Result**: Cross-agent visibility now working. Can see all agent sessions via `sessions_history`.
- **Pending**: Gateway restart needed to apply changes

### Model Configuration
- Current model: `openrouter/z-ai/glm-5` (GLM-5)
- Model aliases defined in config:
  - `opus` → `anthropic/claude-opus-4-5`
  - `sonnet` → `anthropic/claude-sonnet-4-5`
  - `gpt` → `openai/gpt-5.2`
  - `grok-fast` → `openrouter/x-ai/grok-4.1-fast`
  - `OpenRouter` → `openrouter/auto`
- Free models on OpenRouter: `kimi-k2.5`, `glm-4.7`, `glm-5`

## Agent Status (as of 08:45 UTC)
All agents had failed sessions before fix:
- Vibhishana: 3 failed sessions
- Vyasa: 2 failed sessions
- Vidura: 2 failed sessions
- Valmiki: 1 failed session
- Shakti: 2 failed sessions

## Config File Locations
- Main config: `/home/node/.openclaw/openclaw.json`
- Agent models: `/home/node/.openclaw/agents/<agent-id>/agent/models.json`
- Agent auth: `/home/node/.openclaw/agents/<agent-id>/agent/auth-profiles.json`
